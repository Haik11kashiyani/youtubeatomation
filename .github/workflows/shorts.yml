name: Build and Upload YouTube Shorts

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch/commit to run"
        required: false
        default: "main"
      upload:
        description: "Upload to YouTube after rendering (true/false)"
        required: false
        default: "true"

permissions:
  contents: read

jobs:
  disabled-noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop"


